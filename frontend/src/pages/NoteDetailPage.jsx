/* NoteDetailPage.jsx — replaced by TaskDetailPage.
   Redirect kept for safety.
*/
import { Navigate } from "react-router-dom";
export default function NoteDetailPage() {
  return <Navigate to="/tasks" replace />;
}